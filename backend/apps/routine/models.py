# apps.routine does not define its own models.
# It exposes routing, API views, and serializers for the existing RoutineSlot model
# which remains in apps.courses for backward compatibility and database migrations.
